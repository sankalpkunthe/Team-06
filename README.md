# Team-06

1. Firstly I imported necessary libraries which include TensorFlow, Keras, Numpy

2. Then I uploaded Dataset to my colab, then loaded it to my code using ImageDataGenerator

3. I splitted the data into 80% for training and 20% validation. Now I loaded training images and preprocessed them, followed by loading validation images and preprocess them

4. Then comes building CNN with pooling. Now I trained the model using the training set and validate it

5. Now I evaluated trained model on validation set and plotted both training accuracy and validation accuration using matplotlib

6. Now I used a test image and used the model to predict the class and score
